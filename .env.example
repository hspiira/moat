# Copy to .env.local for development. Set the same names in Vercel for deploys.
# NEXT_PUBLIC_ values are shipped to the browser and readable by anyone.
# Everything under "Sync server" is a secret. Never move one group to the other.

# ---------------------------------------------------------------------------
# Web app. Public.
# ---------------------------------------------------------------------------

# Shows the hosted sync controls. Keep it off until sign-in exists; today the
# only way in is a token minted by hand.
NEXT_PUBLIC_ENABLE_HOSTED_SYNC=false

# Google OAuth client id for Drive backup. Leave unset to hide the Drive
# option. File backup and restore work without it.
NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=

# Build output directory. Test tooling only.
# NEXT_DIST_DIR=

# ---------------------------------------------------------------------------
# Sync server. Secret. GET /health names whatever is missing.
# ---------------------------------------------------------------------------

# On Neon use the pooled host, the one with "-pooler".
# Sync tokens live in this database, one per user per device. Make one with:
#   pnpm --filter @moat/sync-server mint mint <userId> [label]
DATABASE_URL=postgres://user:password@host/database

# "disable" for a local database with no TLS. "no-verify" skips certificate
# checks, so only use it if you know why you need it.
# DATABASE_SSL=disable

# Keep small on serverless, where each instance holds its own pool.
# DATABASE_POOL_MAX=10

# Only needed if the app and the sync server are on different domains.
# MOAT_SYNC_ALLOWED_ORIGINS=https://moat.example.com

# Port for local runs. Vercel ignores this.
# PORT=8787

# Tests only. Production uses Postgres.
# MOAT_SYNC_STORE_PATH=

# Sign in with Google, so a device gets a sync token without one being minted by
# hand. Both come from the same Google Cloud OAuth client. The secret is read
# only by the sync server and never reaches the browser.
MOAT_OIDC_GOOGLE_CLIENT_ID=
MOAT_OIDC_GOOGLE_CLIENT_SECRET=

# The iOS app is a separate Google client, of type iOS. It has no secret,
# because an app on every phone could not keep one, and PKCE stands in its
# place. Set the same value in both: the server checks the token was issued for
# this client, and the app sends it as the client asking.
MOAT_OIDC_GOOGLE_IOS_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_IOS_CLIENT_ID=

# Where Google is allowed to send the browser back, for the web only. Comma
# separated, and checked against what the caller asks for, so nobody can have
# the code delivered somewhere they control. The app's own redirect is a scheme
# fixed by its client id, so it needs no entry here.
MOAT_OIDC_REDIRECT_URIS=http://localhost:3000/auth/callback
