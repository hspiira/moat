{
  "name": "@moat/sync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "node build.mjs",
    "vercel-build": "node build.mjs",
    "start": "node server.js",
    "dev": "node build.mjs --watch",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6",
    "esbuild": "^0.28.2"
  }
}
